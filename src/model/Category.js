class Category{
    /**
     * @param {string} name
     * @param {string} color
     */
    constructor(name, color){
        this.name=name;
        //todo: create Section object in backend with name and COLOR
        this.color=color;
    }
}